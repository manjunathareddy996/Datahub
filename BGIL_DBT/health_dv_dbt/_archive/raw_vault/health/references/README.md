Intentionally empty. `REF_RELATIONSHIP_TYPE` and `REF_EXCLUSION` are tagged as used (2 low-confidence
`reference_key` rows) in the Health mapping, but no source table was ever mapped as the
definitional master for either code set — see `docs/HEALTH_DV_BUILD_NOTES.md` §8.
